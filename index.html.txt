```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>I'm So Sorry, My Love</title>
  
  <!-- Tailwind CSS -->
  <script src="https://cdn.tailwindcss.com"></script>
  
  <!-- Google Fonts (Playfair Display for headers, Quicksand for clean body text) -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400..900;1,400..900&family=Quicksand:wght@400;600;700&display=swap" rel="stylesheet">
  
  <!-- Canvas Confetti Library for celebration -->
  <script src="https://cdn.jsdelivr.net/npm/canvas-confetti@1.6.0/dist/confetti.browser.min.js"></script>
  
  <style>
    body {
      font-family: 'Quicksand', sans-serif;
    }
    .font-romantic {
      font-family: 'Playfair Display', serif;
    }
    
    /* Background Heart Animation */
    @keyframes heartFloat {
      0% {
        transform: translateY(110vh) scale(0) rotate(0deg);
        opacity: 0;
      }
      10% {
        opacity: 0.6;
      }
      90% {
        opacity: 0.6;
      }
      100% {
        transform: translateY(-10vh) scale(1.2) rotate(360deg);
        opacity: 0;
      }
    }
    .floating-heart {
      position: absolute;
      color: #f43f5e;
      pointer-events: none;
      animation: heartFloat 7s linear infinite;
      z-index: 0;
    }

    /* Tactile Envelope Styles */
    .envelope-container {
      perspective: 1000px;
    }
    .envelope-card {
      transform-style: preserve-3d;
      transition: transform 0.6s ease;
    }
    
    /* Soft Glow Pulse */
    .pulse-gold {
      animation: goldGlow 2.5s infinite;
    }
    @keyframes goldGlow {
      0%, 100% { box-shadow: 0 0 15px rgba(217, 119, 6, 0.3); }
      50% { box-shadow: 0 0 25px rgba(244, 63, 94, 0.6); }
    }
  </style>
</head>
<body class="bg-gradient-to-tr from-rose-50 via-pink-100 to-rose-100 min-h-screen flex flex-col justify-between overflow-x-hidden relative">

  <!-- Interactive Floating Hearts Background -->
  <div id="hearts-bg" class="absolute inset-0 overflow-hidden pointer-events-none select-none z-0"></div>

  <!-- FLOATING QUICK CUSTOMIZER BUTTON -->
  <div class="fixed bottom-6 left-6 z-40">
    <button onclick="openCustomizer()" class="bg-rose-600 hover:bg-rose-700 active:scale-95 text-white px-4 py-3 rounded-full shadow-2xl flex items-center gap-2 transition duration-300">
      <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15.232 5.232l3.536 3.536m-2.036-5.036a2.5 2.5 0 113.536 3.536L6.5 21.036H3v-3.572L16.732 3.732z" />
      </svg>
      <span class="text-sm font-bold tracking-wide hidden md:inline">Personalize This Apology</span>
    </button>
  </div>

  <!-- CUSTOMIZER MODAL -->
  <div id="customizer-modal" class="fixed inset-0 bg-black/60 backdrop-blur-sm z-50 hidden flex items-center justify-center p-4">
    <div class="bg-white rounded-3xl max-w-md w-full p-6 md:p-8 shadow-2xl border border-rose-100 relative animate-in fade-in duration-300">
      <h3 class="text-2xl font-bold text-rose-800 font-romantic mb-4">Customize Your Message</h3>
      
      <div class="space-y-4">
        <div>
          <label class="block text-xs font-semibold text-rose-600 uppercase tracking-wider">Her Name / Pet Name</label>
          <input type="text" id="input-gf-name" value="My Beautiful Girl" class="w-full mt-1 px-4 py-2 border border-rose-200 rounded-xl focus:outline-none focus:ring-2 focus:ring-rose-400 text-gray-700">
        </div>
        <div>
          <label class="block text-xs font-semibold text-rose-600 uppercase tracking-wider">Your Name</label>
          <input type="text" id="input-your-name" value="Your Loving Boyfriend" class="w-full mt-1 px-4 py-2 border border-rose-200 rounded-xl focus:outline-none focus:ring-2 focus:ring-rose-400 text-gray-700">
        </div>
        <div>
          <label class="block text-xs font-semibold text-rose-600 uppercase tracking-wider">The Primary Apology</label>
          <input type="text" id="input-reason" value="coming late and not being there when you needed me" class="w-full mt-1 px-4 py-2 border border-rose-200 rounded-xl focus:outline-none focus:ring-2 focus:ring-rose-400 text-gray-700">
        </div>
        <div>
          <label class="block text-xs font-semibold text-rose-600 uppercase tracking-wider">Sincere Letter Text</label>
          <textarea id="input-message" rows="4" class="w-full mt-1 px-4 py-2 border border-rose-200 rounded-xl focus:outline-none focus:ring-2 focus:ring-rose-400 text-gray-700 text-sm leading-relaxed">I cannot express how sorry I am for falling short. Time got away from me, and worst of all, I failed to be by your side when you needed support and warmth the most. You deserve my undivided attention and absolute reliability. I promise to do whatever it takes to earn back your beautiful smile and reassure you that you are my number one priority.</textarea>
        </div>
        
        <div class="flex justify-end gap-3 pt-3">
          <button onclick="closeCustomizer()" class="px-4 py-2 text-gray-500 hover:text-gray-700 text-sm font-semibold">Cancel</button>
          <button onclick="applyCustomization()" class="px-6 py-2 bg-rose-600 hover:bg-rose-700 text-white rounded-xl text-sm font-bold shadow-md transition active:scale-95">Save Changes</button>
        </div>
      </div>
    </div>
  </div>

  <!-- HEADER -->
  <header class="w-full py-8 px-4 text-center z-10 select-none">
    <div class="inline-flex items-center gap-2 bg-white/60 px-4 py-1.5 rounded-full border border-rose-200/50 shadow-sm">
      <span class="animate-pulse">🌹</span>
      <span class="text-xs font-bold tracking-widest text-rose-600 uppercase">A Sincere Promise From My Heart</span>
    </div>
  </header>

  <!-- MAIN CONTAINER -->
  <main class="w-full max-w-4xl px-4 mx-auto flex-1 flex flex-col items-center justify-center z-10 py-4">
    
    <!-- STEP 1: THE INTERACTIVE ENVELOPE -->
    <div id="envelope-section" class="w-full max-w-md flex flex-col items-center justify-center py-6 transition duration-500">
      <div class="text-center mb-8 px-4">
        <h2 class="text-3xl md:text-4xl font-romantic text-rose-800 mb-2 font-bold">Hello, <span class="gf-name-text">My Beautiful Girl</span> ✨</h2>
        <p class="text-gray-600 text-sm md:text-base">I built this little digital space to say how sorry I am. Please tap the letter inside the envelope to read it.</p>
      </div>

      <button onclick="openLetter()" class="envelope-container relative group focus:outline-none py-6 select-none" aria-label="Open Letter">
        <!-- Interactive Envelope -->
        <div class="envelope-card bg-rose-100 hover:scale-105 transform transition duration-500 w-80 h-56 rounded-b-2xl rounded-t-sm shadow-2xl border-t border-rose-200/60 flex items-center justify-center relative pulse-gold">
          
          <!-- Wax Seal (Click trigger) -->
          <div class="absolute w-16 h-16 bg-gradient-to-br from-rose-500 to-rose-700 rounded-full flex items-center justify-center shadow-lg transform -translate-y-2 z-20 group-hover:scale-110 transition duration-300 border-2 border-rose-300">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8 text-rose-100 animate-pulse" fill="currentColor" viewBox="0 0 24 24">
              <path d="M12 4.419c-2.826-5.695-11.999-4.064-11.999 3.27 0 7.27 9.903 10.938 11.999 15.311 2.096-4.373 12-8.041 12-15.311 0-7.327-9.17-8.972-12-3.27z"/>
            </svg>
          </div>
          
          <!-- Top Envelope Flap -->
          <div class="absolute top-0 inset-x-0 h-0 w-0 border-t-[110px] border-t-rose-300/90 border-x-[160px] border-x-transparent origin-top transition duration-500 z-10"></div>
          
          <!-- Letter poking out slightly -->
          <div class="absolute top-3 w-72 h-44 bg-white rounded-xl shadow-inner transform translate-y-2 transition group-hover:-translate-y-6 duration-500 z-0 flex flex-col items-center justify-center p-4 border border-rose-50">
            <p class="font-romantic text-rose-700 italic text-center text-sm font-semibold">"I made a mistake, and I want to make it up to you..."</p>
            <span class="text-xs text-rose-400 mt-2 font-bold uppercase tracking-wider">Tap to read</span>
          </div>
        </div>
      </button>
    </div>

    <!-- STEP 2: THE DETAILED APOLOGY & INTERACTION PACK -->
    <div id="content-section" class="hidden w-full space-y-12 pb-24 transition duration-700">
      
      <!-- Part A: The Sincere Letter -->
      <div class="bg-white/95 backdrop-blur-md p-6 md:p-12 rounded-3xl shadow-2xl border border-rose-100 relative overflow-hidden max-w-2xl mx-auto">
        <!-- Floating Rose Graphic Background -->
        <div class="absolute -right-8 -bottom-8 opacity-5 pointer-events-none select-none">
          <svg xmlns="http://www.w3.org/2000/svg" width="220" height="220" fill="currentColor" class="text-rose-600" viewBox="0 0 16 16">
            <path d="M8 1.27c.5 0 5.55 1.5 5.55 6.43 0 1.22-.38 2.5-1.07 3.6-.82 1.3-2.03 2.37-3.66 3.16-.25.12-.57.12-.82 0-1.63-.79-2.84-1.86-3.66-3.16-.7-1.1-.1-2.38-1.07-3.6C2.45 2.77 7.5 1.27 8 1.27zm0 1.13c-.3 0-4.32 1.25-4.32 5.17 0 1 .31 2.05.88 2.97.68 1.08 1.69 1.97 3.04 2.63a.5.5 0 0 0 .4 0c1.35-.66 2.36-1.55 3.04-2.63.57-.92.88-1.97.88-2.97 0-3.92-4.02-5.17-4.32-5.17z"/>
          </svg>
        </div>

        <div class="text-center mb-8">
          <span class="text-rose-400 text-5xl font-romantic block leading-none select-none">“</span>
          <h2 class="text-3xl md:text-4xl font-romantic text-rose-800 font-bold px-2 inline-block -mt-4">My Sincerest Apology</h2>
        </div>

        <div class="prose text-gray-700 leading-relaxed space-y-6 text-base md:text-lg">
          <p>Dearest <strong class="gf-name-text text-rose-700 font-bold">My Beautiful Girl</strong>,</p>
          
          <p>Writing this is my way of putting my feelings transparently in front of you. I messed up, and I want to own every single bit of it without excuses.</p>
          
          <p>I know I was late, and even worse, <strong class="text-rose-600 font-semibold">I wasn't there for you when you needed me most</strong>. You should never have to feel like you are facing things alone, especially when I should be your strongest anchor.</p>
          
          <!-- Customizable Core Letter Text Block -->
          <div class="p-5 md:p-6 bg-rose-50/80 border-l-4 border-rose-500 rounded-r-2xl italic text-rose-950 font-romantic text-base md:text-lg my-6 shadow-sm leading-relaxed" id="dynamic-message">
            "I cannot express how sorry I am for falling short. Time got away from me, and worst of all, I failed to be by your side when you needed support and warmth the most. You deserve my undivided attention and absolute reliability. I promise to do whatever it takes to earn back your beautiful smile and reassure you that you are my number one priority."
          </div>

          <p>Your peace of mind and trust are everything to me. I hate knowing I caused even a single moment of worry or disappointment. I want to build a safe, loving, and unconditionally supportive space with you.</p>
          
          <div class="pt-6 border-t border-rose-100 flex flex-col items-end">
            <p class="font-romantic text-lg text-rose-900">Always and forever yours,</p>
            <p class="font-romantic text-xl font-bold text-rose-700 mt-1 your-name-text">Your Loving Boyfriend</p>
          </div>
        </div>
      </div>

      <!-- Part B: Interactive Commitments Grid -->
      <div class="max-w-3xl mx-auto space-y-6">
        <h3 class="text-2xl font-romantic text-center text-rose-800 font-bold mb-2">My Commitments To You</h3>
        <p class="text-center text-sm text-gray-600 max-w-md mx-auto -mt-4">These aren't just empty declarations. These are my direct rules of action moving forward.</p>
        
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mt-6">
          <!-- Card 1 -->
          <div class="bg-white/80 p-6 rounded-2xl border border-rose-100 shadow-md hover:shadow-xl transition duration-300">
            <div class="w-12 h-12 bg-rose-100 rounded-full flex items-center justify-center text-rose-600 mb-4 font-bold text-lg">1</div>
            <h4 class="font-bold text-rose-900 text-lg mb-2">Always Present</h4>
            <p class="text-sm text-gray-600 leading-relaxed">No matter how busy life gets, when you need me near or require support, I will show up. You are my highest priority, period.</p>
          </div>

          <!-- Card 2 -->
          <div class="bg-white/80 p-6 rounded-2xl border border-rose-100 shadow-md hover:shadow-xl transition duration-300">
            <div class="w-12 h-12 bg-rose-100 rounded-full flex items-center justify-center text-rose-600 mb-4 font-bold text-lg">2</div>
            <h4 class="font-bold text-rose-900 text-lg mb-2">Absolute Time Respect</h4>
            <p class="text-sm text-gray-600 leading-relaxed">No more running late. I will budget my time to arrive early and ensure your schedules and comfort are never disrupted.</p>
          </div>

          <!-- Card 3 -->
          <div class="bg-white/80 p-6 rounded-2xl border border-rose-100 shadow-md hover:shadow-xl transition duration-300">
            <div class="w-12 h-12 bg-rose-100 rounded-full flex items-center justify-center text-rose-600 mb-4 font-bold text-lg">3</div>
            <h4 class="font-bold text-rose-900 text-lg mb-2">Attentive Anticipation</h4>
            <p class="text-sm text-gray-600 leading-relaxed">I will keep better track of what is going on in your life so I can anticipate when you need a helping hand before you have to ask.</p>
          </div>

          <!-- Card 4 -->
          <div class="bg-white/80 p-6 rounded-2xl border border-rose-100 shadow-md hover:shadow-xl transition duration-300">
            <div class="w-12 h-12 bg-rose-100 rounded-full flex items-center justify-center text-rose-600 mb-4 font-bold text-lg">4</div>
            <h4 class="font-bold text-rose-900 text-lg mb-2">Honest, Early Communication</h4>
            <p class="text-sm text-gray-600 leading-relaxed">If anything unexpected arises, I will communicate transparently and instantly, keeping you in the loop and valued.</p>
          </div>
        </div>
      </div>

      <!-- Part C: Redeemable Love Vouchers -->
      <div class="max-w-3xl mx-auto space-y-6">
        <div class="text-center">
          <h3 class="text-2xl font-romantic text-rose-800 font-bold">Apology Compensation Coupons</h3>
          <p class="text-sm text-gray-600 mt-2">Tap any voucher below to claim/activate it instantly! (Valid forever, non-refundable 💖)</p>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
          <!-- Coupon 1 -->
          <div id="coupon-1" class="bg-gradient-to-br from-rose-100 to-pink-50 p-6 rounded-3xl border-2 border-dashed border-rose-300/70 shadow-md relative overflow-hidden transition-all duration-300 hover:shadow-lg">
            <div class="absolute -top-4 -right-4 w-12 h-12 bg-rose-200 rounded-full opacity-30"></div>
            <span class="text-xs font-bold text-rose-500 tracking-widest uppercase">Voucher #01</span>
            <h4 class="font-bold text-rose-900 text-xl mt-1">One Infinite Hug</h4>
            <p class="text-xs text-rose-700 mt-2 leading-relaxed">Redeemable for an ultra-tight, warm embrace that lasts exactly as long as you want.</p>
            <button onclick="redeemCoupon('coupon-1', 'One Infinite Hug')" class="mt-5 w-full bg-rose-600 hover:bg-rose-700 text-white text-xs font-bold py-2.5 rounded-xl transition duration-200 shadow-sm">
              Redeem Now
            </button>
          </div>

          <!-- Coupon 2 -->
          <div id="coupon-2" class="bg-gradient-to-br from-rose-100 to-pink-50 p-6 rounded-3xl border-2 border-dashed border-rose-300/70 shadow-md relative overflow-hidden transition-all duration-300 hover:shadow-lg">
            <span class="text-xs font-bold text-rose-500 tracking-widest uppercase">Voucher #02</span>
            <h4 class="font-bold text-rose-900 text-xl mt-1">Late-Proof Dinner</h4>
            <p class="text-xs text-rose-700 mt-2 leading-relaxed">A gorgeous romantic date of your choice where I guarantee arriving 15 minutes early.</p>
            <button onclick="redeemCoupon('coupon-2', 'Late-Proof Dinner')" class="mt-5 w-full bg-rose-600 hover:bg-rose-700 text-white text-xs font-bold py-2.5 rounded-xl transition duration-200 shadow-sm">
              Redeem Now
            </button>
          </div>

          <!-- Coupon 3 -->
          <div id="coupon-3" class="bg-gradient-to-br from-rose-100 to-pink-50 p-6 rounded-3xl border-2 border-dashed border-rose-300/70 shadow-md relative overflow-hidden transition-all duration-300 hover:shadow-lg">
            <span class="text-xs font-bold text-rose-500 tracking-widest uppercase">Voucher #03</span>
            <h4 class="font-bold text-rose-900 text-xl mt-1">Spa & Relaxation</h4>
            <p class="text-xs text-rose-700 mt-2 leading-relaxed">Full back massage, dynamic foot rubbing, and your favorite treats served on demand.</p>
            <button onclick="redeemCoupon('coupon-3', 'Spa & Relaxation')" class="mt-5 w-full bg-rose-600 hover:bg-rose-700 text-white text-xs font-bold py-2.5 rounded-xl transition duration-200 shadow-sm">
              Redeem Now
            </button>
          </div>
        </div>
      </div>

      <!-- Part D: Interactive Forgiveness Zone -->
      <div id="forgiveness-card" class="max-w-md mx-auto bg-white/95 p-8 rounded-3xl shadow-2xl border border-rose-100 text-center transition duration-500 relative">
        <h3 class="text-2xl font-romantic text-rose-800 font-bold mb-2">The Ultimate Verdict</h3>
        <p class="text-sm text-gray-500 mb-8">Do you forgive your deeply sorry partner?</p>
        
        <div class="flex flex-col sm:flex-row items-center justify-center gap-4 relative min-h-[140px] sm:min-h-[80px]" id="verdict-buttons-container">
          <!-- YES button (Spawns Confetti & Love Screen) -->
          <button id="yes-btn" onclick="forgiveYes()" class="px-8 py-3 bg-emerald-500 hover:bg-emerald-600 active:scale-95 text-white font-bold rounded-full shadow-lg transition-all duration-300 text-lg z-10 w-full sm:w-auto">
            Yes, I forgive you! 💕
          </button>
          
          <!-- NO button (Dojos / Moves Away Playfully) -->
          <button id="no-btn" onmouseover="dodgeNoButton()" onclick="dodgeNoButton()" ontouchstart="dodgeNoButton(); event.preventDefault();" class="px-6 py-2.5 bg-rose-500 hover:bg-rose-600 text-white font-bold rounded-full shadow-md transition-all duration-200 text-sm absolute z-20 w-32">
            No 😤
          </button>
        </div>
      </div>

    </div>

    <!-- STEP 3: CELEBRATION SCREEN -->
    <div id="celebration-section" class="hidden text-center max-w-lg mx-auto py-12 px-6 space-y-6 animate-fade-in">
      <div class="w-24 h-24 bg-rose-100 rounded-full flex items-center justify-center mx-auto text-rose-600 text-5xl animate-bounce shadow-inner">
        🥰
      </div>
      <h2 class="text-3xl md:text-4xl font-romantic text-rose-900 font-bold">You are the absolute best!</h2>
      <p class="text-gray-700 text-base md:text-lg leading-relaxed">
        Thank you for your beautiful heart and understanding. I promise to keep every single one of my promises and protect your happiness forever. I am coming over to give you the biggest cuddle in history!
      </p>
      
      <!-- Voucher notification details -->
      <div class="p-5 bg-white rounded-3xl shadow-md border border-rose-100 max-w-md mx-auto">
        <p class="text-xs font-bold text-rose-500 uppercase tracking-widest">Active Redemption Status</p>
        <p class="text-sm text-rose-800 mt-2 font-semibold">Please present this screen to claim your first compensation hug instantly! 🎟️</p>
      </div>

      <div class="pt-6">
        <button onclick="resetApology()" class="text-sm text-rose-600 hover:text-rose-800 font-bold underline transition">
          Go Back & View Letter Again
        </button>
      </div>
    </div>

  </main>

  <!-- CUSTOM MODAL NOTIFICATION -->
  <div id="alert-modal" class="fixed inset-0 bg-black/50 backdrop-blur-sm z-50 hidden flex items-center justify-center p-4">
    <div class="bg-white rounded-3xl max-w-sm w-full p-6 shadow-2xl text-center border border-rose-50 animate-in fade-in duration-200">
      <div class="text-rose-500 text-5xl mb-3">💝</div>
      <h4 class="text-xl font-bold text-rose-900 font-romantic mb-2" id="alert-title">Notification</h4>
      <p class="text-gray-600 text-sm leading-relaxed mb-6" id="alert-msg">Details go here.</p>
      <button onclick="closeCustomAlert()" class="w-full py-3 bg-rose-600 hover:bg-rose-700 text-white font-bold rounded-2xl text-sm shadow-md transition active:scale-95">
        Awesome!
      </button>
    </div>
  </div>

  <!-- FOOTER -->
  <footer class="w-full py-6 text-center text-xs text-rose-500/80 font-bold tracking-wider z-10 select-none">
    <p>Created with Love & Absolute Sincerity • <span class="your-name-text">Your Loving Boyfriend</span></p>
  </footer>

  <!-- SCRIPT LOGIC -->
  <script>
    // State storage variables
    let gfName = "My Beautiful Girl";
    let yourName = "Your Loving Boyfriend";
    let reason = "coming late and not being there when you needed me";
    let message = "I cannot express how sorry I am for falling short. Time got away from me, and worst of all, I failed to be by your side when you needed support and warmth the most. You deserve my undivided attention and absolute reliability. I promise to do whatever it takes to earn back your beautiful smile and reassure you that you are my number one priority.";

    let dodgeCount = 0;

    window.onload = function() {
      // Create animated floating hearts in background
      initHeartsBackground();
      // Apply correct initial names and labels
      syncInterfaceTexts();
      // Position the runaway NO button initially
      resetNoButtonPosition();
    };

    // Update screen components with custom data
    function syncInterfaceTexts() {
      document.querySelectorAll('.gf-name-text').forEach(el => el.textContent = gfName);
      document.querySelectorAll('.your-name-text').forEach(el => el.textContent = yourName);
      
      const dynamicMsg = document.getElementById('dynamic-message');
      if (dynamicMsg) {
        dynamicMsg.innerHTML = `"${message}"`;
      }
    }

    // Open/Close settings customizer modal
    function openCustomizer() {
      document.getElementById('input-gf-name').value = gfName;
      document.getElementById('input-your-name').value = yourName;
      document.getElementById('input-reason').value = reason;
      document.getElementById('input-message').value = message;
      document.getElementById('customizer-modal').classList.remove('hidden');
    }

    function closeCustomizer() {
      document.getElementById('customizer-modal').classList.add('hidden');
    }

    // Save and apply customization settings
    function applyCustomization() {
      gfName = document.getElementById('input-gf-name').value.trim() || "My Beautiful Girl";
      yourName = document.getElementById('input-your-name').value.trim() || "Your Loving Boyfriend";
      reason = document.getElementById('input-reason').value.trim() || "coming late";
      message = document.getElementById('input-message').value.trim() || "I am so sorry...";

      syncInterfaceTexts();
      closeCustomizer();
      
      showCustomAlert("Details Updated! 💕", "Your personalized touch has been applied. Show this page to her now!");
    }

    // Custom non-blocking modal alert
    function showCustomAlert(title, messageText) {
      document.getElementById('alert-title').textContent = title;
      document.getElementById('alert-msg').textContent = messageText;
      document.getElementById('alert-modal').classList.remove('hidden');
    }

    function closeCustomAlert() {
      document.getElementById('alert-modal').classList.add('hidden');
    }

    // Generate floating hearts background
    function initHeartsBackground() {
      const bg = document.getElementById('hearts-bg');
      const symbols = ['❤️', '💖', '💝', '💕', '🌹', '✨'];
      
      setInterval(() => {
        const heart = document.createElement('div');
        heart.classList.add('floating-heart');
        heart.innerHTML = symbols[Math.floor(Math.random() * symbols.length)];
        heart.style.left = Math.random() * 100 + 'vw';
        heart.style.fontSize = Math.random() * 24 + 12 + 'px';
        heart.style.animationDuration = Math.random() * 5 + 4 + 's'; // 4s to 9s
        bg.appendChild(heart);

        // Clean up heart after completion
        setTimeout(() => {
          heart.remove();
        }, 9000);
      }, 600);
    }

    // Open Envelope Action
    function openLetter() {
      document.getElementById('envelope-section').classList.add('hidden');
      const content = document.getElementById('content-section');
      content.classList.remove('hidden');
      content.scrollIntoView({ behavior: 'smooth' });
    }

    // Interactive love vouchers claiming
    function redeemCoupon(id, name) {
      const card = document.getElementById(id);
      
      // Visually stamp it redeemed
      card.classList.remove('from-rose-100', 'to-pink-50');
      card.classList.add('from-gray-150', 'to-gray-200', 'opacity-70', 'border-gray-400');
      
      const btn = card.querySelector('button');
      btn.disabled = true;
      btn.innerHTML = "✓ Active & Redeemed";
      btn.classList.remove('bg-rose-600', 'hover:bg-rose-700');
      btn.classList.add('bg-gray-500', 'cursor-not-allowed');

      showCustomAlert(
        "Voucher Activated! 🎟️", 
        `"${name}" is officially locked in! I am fully committed to delivering this whenever you decide to cash it in.`
      );
    }

    // Playful runaway "No" button routine
    function dodgeNoButton() {
      const noBtn = document.getElementById('no-btn');
      const yesBtn = document.getElementById('yes-btn');
      const container = document.getElementById('forgiveness-card');

      const playfulLines = [
        "Are you sure? 🥺",
        "Give me a chance! 🙏",
        "Wait, think about it! 💔",
        "No isn't an option! 😜",
        "I will buy you snacks! 🍩",
        "Please? 😭",
        "How about now? ⭐",
        "System Error: Try 'Yes'!"
      ];

      dodgeCount++;

      // Change label randomly to make her smile
      noBtn.innerText = playfulLines[Math.floor(Math.random() * playfulLines.length)];

      // Make the "Yes" button progressively larger with every dodge!
      const currentScale = 1 + (dodgeCount * 0.12);
      yesBtn.style.transform = `scale(${Math.min(currentScale, 2.2)})`;

      // Get safe bound boundaries relative to the parenting forgiveness box
      const containerRect = container.getBoundingClientRect();
      const noRect = noBtn.getBoundingClientRect();

      // Generate localized random positions
      const maxX = containerRect.width - noRect.width - 24;
      const maxY = containerRect.height - noRect.height - 24;

      const randomX = Math.random() * maxX;
      const randomY = Math.random() * (maxY - 40) + 40; // Avoid hitting header line closely

      noBtn.style.left = `${randomX}px`;
      noBtn.style.top = `${randomY}px`;
    }

    // Resets "No" button relative position to original space
    function resetNoButtonPosition() {
      const noBtn = document.getElementById('no-btn');
      noBtn.style.position = 'absolute';
      noBtn.style.left = '60%';
      noBtn.style.top = '24px';
    }

    // Success "YES" trigger
    function forgiveYes() {
      // Fire confetti celebration loops
      const end = Date.now() + (4 * 1000);
      const colors = ['#f43f5e', '#ec4899', '#f43f5e', '#fb7185', '#ffffff'];

      (function frame() {
        confetti({
          particleCount: 5,
          angle: 60,
          spread: 55,
          origin: { x: 0 },
          colors: colors
        });
        confetti({
          particleCount: 5,
          angle: 120,
          spread: 55,
          origin: { x: 1 },
          colors: colors
        });

        if (Date.now() < end) {
          requestAnimationFrame(frame);
        }
      }());

      // Hide apology card, display celebration screens
      document.getElementById('content-section').classList.add('hidden');
      const celebSec = document.getElementById('celebration-section');
      celebSec.classList.remove('hidden');
      celebSec.scrollIntoView({ behavior: 'smooth' });
    }

    // Restart process
    function resetApology() {
      document.getElementById('celebration-section').classList.add('hidden');
      document.getElementById('content-section').classList.remove('hidden');
      
      // Reset dodging counters & button scaling
      dodgeCount = 0;
      const yesBtn = document.getElementById('yes-btn');
      yesBtn.style.transform = 'scale(1)';
      const noBtn = document.getElementById('no-btn');
      noBtn.innerText = "No 😤";
      resetNoButtonPosition();

      // Restore all interactive love coupons to functional states
      const coupons = ['coupon-1', 'coupon-2', 'coupon-3'];
      coupons.forEach((couponId) => {
        const card = document.getElementById(couponId);
        card.classList.remove('from-gray-150', 'to-gray-200', 'opacity-70', 'border-gray-400');
        card.classList.add('from-rose-100', 'to-pink-50');
        const btn = card.querySelector('button');
        btn.disabled = false;
        btn.innerHTML = "Redeem Now";
        btn.classList.add('bg-rose-600', 'hover:bg-rose-700');
        btn.classList.remove('bg-gray-500', 'cursor-not-allowed');
      });

      document.getElementById('content-section').scrollIntoView({ behavior: 'smooth' });
    }
  </script>
</body>
</html>

```
